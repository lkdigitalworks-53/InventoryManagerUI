{
  "name": "businessmanagement-functions",
  "description": "Compliance gateway Cloud Functions for the BusinessManagement App_UI (P0).",
  "engines": {
    "node": "20"
  },
  "main": "index.js",
  "scripts": {
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log",
    "test": "node --test"
  },
  "dependencies": {
    "firebase-admin": "^12.6.0",
    "firebase-functions": "^7.3.2-rc.0"
  },
  "private": true,
  "devDependencies": {
    "node-mocks-http": "^1.18.1"
  }
}
