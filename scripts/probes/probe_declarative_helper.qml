// Loaded by probe_dp_sp_outside_app_root.qml via Qt.createComponent(), not
// meant to be run standalone. See that file's header comment for context.
import QtQuick

Item {
    property real probedWidth: dp(16)
    property real probedFontSize: sp(16)
}
